# ARP-Address-Resolution-Protocol-implementation
A low-level implementation of the Address Resolution Protocol (ARP) in C for Linux.  Demonstrates raw socket programming, packet construction, and network interface  manipulation to discover MAC addresses from IP addresses on a LAN.
